#!/usr/bin/env bash
set -euo pipefail

# Usage: ./install

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$script_dir"

die() { echo "install: $*" >&2; exit 1; }
info() { echo "==> $*"; }
